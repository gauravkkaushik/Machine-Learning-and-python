# Package a function

In this directory is a python file containing a simple function. Convert this function into a python package named `simple` so you can run `from simple.function import simple_function` 

**Complete the following steps:**

- Add the required folder structure and files to this directory
- Build the distribution for the package
- Install the package

## Test your code

**Run the following command in your terminal:**

```bash
pytest
```