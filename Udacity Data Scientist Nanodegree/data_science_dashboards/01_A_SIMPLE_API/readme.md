# A simple FastHTML API

1. Create a '/' endpoint that returns the string 'Hello World!'
2. Create '/speakers/' endpoint that returns the data for every speaking character in Romeo and Juliet

# Dynamic Endpoint

- Alter the '/speaker/' endpoint so we can request the dat for a specific character.

# Query Parameters

- Add query parameters to the `/speaker/` endpoint that allow data to be filters by the `act` and `scene` of the play.